# 📝 HOMEWORK: After Learning Lists (Stage 1)

## What to do at home:

After today's lesson on React lists, complete this simple task list project.

### Your Task:

Create a simple task list that displays tasks using `.map()`

### Steps:

1. **Create an array of tasks**

   ```jsx
   const tasks = ["Do homework", "Clean room", "Read a book", "Play outside"];
   ```

2. **Display them using .map()**

   - Use `.map()` to create a list
   - Don't forget the `key` prop!
   - Add an emoji or checkbox before each task

3. **Make it look nice**
   - Style your list with colors
   - Make each task look like a card

### What you'll learn:

- ✅ Practice using `.map()` with arrays
- ✅ Understanding the `key` prop
- ✅ Basic styling in React

### Next lesson:

We'll add the ability to:

- ➕ Add new tasks
- 🗑️ Delete tasks
- 📁 Organize tasks into categories (Work, School, Personal)
- 🔄 Move tasks between categories

---

**Time needed:** 30-45 minutes at home
